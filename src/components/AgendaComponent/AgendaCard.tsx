import React, { ReactElement } from "react"
import Card from "../common/Card/Card"
interface Props {}

function AgendaCard({}: Props): ReactElement {
  return <Card></Card>
}

export default AgendaCard

/// Styled
